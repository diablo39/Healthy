namespace Healthy.Core.Engine.Outputs
{
    public abstract class OutputBase
    {
        // consider output as
        // * http endpoint (healthcheck, panel)
        // * http request (webhook, heartbeat)
    }
}